//Mixin für die Berechnungen
export default {
    methods: {
        addieren: function (a, b) {
            return a + b;
        },
        subtrahieren: function (a, b) {
            return a - b;
        },
        multiplizieren: function (a, b) {
            return a * b;
        },
        dividieren: function (a, b) {
            return a / b;
        }
    }
}